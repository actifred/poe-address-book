import { AlterncasePipe } from './alterncase.pipe';

describe('AlterncasePipe', () => {
  it('create an instance', () => {
    const pipe = new AlterncasePipe();
    expect(pipe).toBeTruthy();
  });
});
